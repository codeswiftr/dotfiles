# Pacman packages for Arch — platform only
# Usage: grep -v '^#' pacman.txt | grep -v '^$' | sudo pacman -S --noconfirm -
# Pinned CLIs → mise.toml; herdr/mise/AI → install.sh helpers

zsh
git
curl
wget
neovim
eza
mosh
base-devel
cmake
python
python-pip
htop
tree
