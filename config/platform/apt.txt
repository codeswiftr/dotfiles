# APT packages for Debian/Ubuntu — platform only
# Usage: grep -v '^#' apt.txt | grep -v '^$' | xargs sudo apt install -y
# Pinned CLIs (rg, bat, fd, fzf, starship, …) → mise.toml
# herdr / chezmoi / neovim / uv / bun / AI → scripts in install.sh

zsh
git
curl
wget
mosh
build-essential
ca-certificates
cmake
pkg-config
python3-dev
python3-pip
python3-venv
htop
tree
