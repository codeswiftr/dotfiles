# Modular architecture

**Superseded.** Layout and module ownership live in [ARCHITECTURE.md](../ARCHITECTURE.md).

Shell load order and modes: `.zshrc` + `config/zsh/*` (see ARCHITECTURE “Shell Modes”).
Config map: [configuration.md](configuration.md).

This file remains only so old links do not 404.
